acá arranqué con el grid density con un isovalue de 1

// python2.7 ../../../fpocket2/scripts/extractISOPdb.py mdpout_dens_grid.dx os.pdb 1.0

y luego fui depurando
el .pdb de salida (del script de mdpocket), limpiando los grids q no quería 
usar. P/ seleccionar los atomos en el pymol

// select wachin, mdpout_freq_iso_0_5 within 5 of at*
y luego:
// run wachin_iterator.py

Fuí depurando os.pdb hasta llegar a 8os.pdb q es final_pocket.pdb
El script lo fui depurando con el siguiente comando:

siendo "temp" el archivo con los átomos a eliminar y en el formato de sed
